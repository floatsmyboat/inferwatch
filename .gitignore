# virtualenv
.venv/
venv/

# python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# machine-specific: generated by scripts/install-mcp.sh
.mcp.json

# metrics database (may contain client IPs from your network)
*.db
*.db-wal
*.db-shm
inferwatch.db*

# settings live in the SQLite database, not a file; this is only here in case a
# future build writes one, and to catch a stray local config
config.json
.claude/settings.local.json
